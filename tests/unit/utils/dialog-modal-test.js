import dialogModal from '../../../utils/dialog-modal';
import { module, test } from 'qunit';

module('Unit | Utility | dialog modal');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dialogModal();
  assert.ok(result);
});
