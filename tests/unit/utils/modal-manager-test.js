import modalManager from '../../../utils/modal-manager';
import { module, test } from 'qunit';

module('Unit | Utility | modal manager');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = modalManager();
  assert.ok(result);
});
